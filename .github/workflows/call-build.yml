name: Call Build

on:
    workflow_dispatch:

    push:
        branches: [main]

jobs:
    Build:
      uses: ./.github/workflows/reusable-build.yml
      with:
        app_name: "my-web-app"
        environment: "production"
      secrets:
        docker_token: ${{ secrets.DOCKER_TOKEN }}
        